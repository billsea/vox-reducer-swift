# Uncomment the next line to define a global platform for your project
 platform :ios, '13.0'

target 'VoxReducer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VoxReducer
  #pod 'Google-Mobile-Ads-SDK'
  pod "Rollbar", "~> 0.2"
  pod 'Firebase/Core'

  target 'VoxReducerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VoxReducerUITests' do
    # Pods for testing
  end

end
